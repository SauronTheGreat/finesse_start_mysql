<%= form_for(@consumer_media_preference) do |f| %>
  <% if @consumer_media_preference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@consumer_media_preference.errors.count, "error") %> prohibited this consumer_media_preference from being saved:</h2>

      <ul>
      <% @consumer_media_preference.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :media_id %><br />
    <%= f.number_field :media_id %>
  </div>
  <div class="field">
    <%= f.label :consumer_category_id %><br />
    <%= f.number_field :consumer_category_id %>
  </div>
  <div class="field">
    <%= f.label :index %><br />
    <%= f.number_field :index %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

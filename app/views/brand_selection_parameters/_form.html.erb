<%= form_for(@brand_selection_parameter) do |f| %>
  <% if @brand_selection_parameter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@brand_selection_parameter.errors.count, "error") %> prohibited this brand_selection_parameter from being saved:</h2>

      <ul>
      <% @brand_selection_parameter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

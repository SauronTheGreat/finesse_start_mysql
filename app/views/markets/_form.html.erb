<%= form_for(@market) do |f| %>
    <% if @market.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@market.errors.count, "error") %> prohibited this market from being saved:</h2>

          <ul>
            <% @market.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <div class="field">
      <%= f.label :name %>
      <br/>
      <%= f.text_field :name %>
    </div>
    <div class="field">
      <%= f.label :catchment_of_dealers %>
      <br/>
      <%= f.number_field :catchment_of_dealers %>
    </div>
    <div class="field">
      <%= f.label :catchment_of_vendors %>
      <br/>
      <%= f.number_field :catchment_of_vendors %>
    </div>

    <div class="field">
      <%= f.label :no_of_consumers %>
      <br/>
      <%= f.number_field :no_of_consumers %>
    </div>




    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>

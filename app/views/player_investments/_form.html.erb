<%= form_for(@player_investment) do |f| %>
  <% if @player_investment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@player_investment.errors.count, "error") %> prohibited this player_investment from being saved:</h2>

      <ul>
      <% @player_investment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :player_id %><br />
    <%= f.number_field :player_id %>
  </div>
  <div class="field">
    <%= f.label :investment_id %><br />
    <%= f.number_field :investment_id %>
  </div>
  <div class="field">
    <%= f.label :amount %><br />
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

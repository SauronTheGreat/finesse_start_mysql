<%= form_for(@dealer_push_index) do |f| %>
  <% if @dealer_push_index.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dealer_push_index.errors.count, "error") %> prohibited this dealer_push_index from being saved:</h2>

      <ul>
      <% @dealer_push_index.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :dealer_id %><br />
    <%= f.number_field :dealer_id %>
  </div>
  <div class="field">
    <%= f.label :brand_id %><br />
    <%= f.number_field :brand_id %>
  </div>
  <div class="field">
    <%= f.label :push_index_value %><br />
    <%= f.number_field :push_index_value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

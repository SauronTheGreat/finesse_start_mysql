<h1>Listing products</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Number Of Parameters</th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
    <th></th>


  </tr>

<% @products.each do |product| %>
  <tr>
    <td><%= product.name %></td>
    <td><%= product.number_of_parameters %></td>

    <td><%= link_to 'Show', product %></td>
    <td><%= link_to 'Edit', edit_product_path(product) %></td>
    <td><%= link_to 'Set Parameters', new_product_parameter_path(:product_id=>product.id) %></td>
    <td><%= link_to 'View Parameters', view_parameters_path(:product_id=>product.id) %></td>


    <td><%= link_to 'Destroy', product, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Product', new_product_path %>
